{
  "name": "garrettstimpson-agent",
  "version": "1.0.0",
  "private": true,
  "description": "llms.txt-powered research agent — deploy on Cloudflare Workers AI (free tier)",
  "cloudflare": {
    "bindings": {
      "LLMS_URL": {
        "description": "URL to your site's `llms.txt` corpus file (e.g. `https://yoursite.com/llms.txt`)"
      },
      "SITE_NAME": {
        "description": "Your site's display name shown in the agent chat UI"
      }
    }
  }
}

# Copy to .env and set a long random value:  openssl rand -hex 24
BROKER_TOKEN=change-me-to-a-long-random-secret
# Keep false in production. HTTP sample downloads are only for isolated lab hosts.
BROKER_ALLOW_HTTP_SAMPLES=false
